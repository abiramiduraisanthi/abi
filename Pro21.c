#include<stdio.h>
int main()
{
int i,j,n=0,t,sum=0;
printf("\n enter the first element:");
scanf("%d",&a)
printf("\n enter the difference \n enter the count:")
scanf("%d%d",&d,&n);
for(i=0;i<=n;i++)
{
if(t==n)
{
sum=sum+i;
}
printf("%d",sum);
}
return 0;
}
