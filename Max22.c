#include<stdio.h>
int main()
{
int n,k,i,a[100];
printf("\n enter the count:");
scanf("%d",&n);
printf("\n enter the array of elements:");
scanf("%d",&a[i]);
for(i=0;i<=n;i++)
{
scanf("%d",&a[i]);
if(a[i]>=k)
{
k=a[i];
}
printf("%d\n",k);
}
return 0;
}
