#include<stdio.h>
int main()
{
int n,a[100],i,temp;
printf("\n enter the count:");
scanf("%d",&n);
printf("\n enter the elements:");
scanf("%d",&a[i]);
temp=a[i];
for(i=2;i<=n;i++)
{
scanf("%d",&a[1]);
}
if(a[i]<=temp)
{
temp=a[i];
}
}
printf("%d\n",temp);
return 0;
}
